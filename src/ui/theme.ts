import { extendTheme } from '@chakra-ui/react';

/**
 * App theme.
 */
export const appTheme = extendTheme({
  fonts: {
    heading: 'Poppins',
    body: 'Poppins',
  },
  colors: {
    primary: {
      500: '#FF5050',
      600: '#f53636',
    },
  },
});
